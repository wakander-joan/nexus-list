spring:
  application:
    name: nexus-list

  datasource:
    url: jdbc:postgresql://hopper.proxy.rlwy.net:45622/railway
    username: postgres
    password: hHdrHprNkUhgvbkBckJpDGlonIFGXhhR
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    url: http://localhost:8080/v3/api-docs
  servers:
    - url: http://localhost:8080

jwt:
  secret: dGVzdGVTZWNyZXRLZXlQYXJhTmV4dXNMaXN0QXBwbGljYXRpb25KV1QyMDI0
  expiration: 86400000

aws:
  access-key: ${AWS_ACCESS_KEY_ID}
  secret-key: ${AWS_SECRET_ACCESS_KEY}
  region: ${AWS_REGION}
  bucket-name: ${AWS_BUCKET_NAME}
  presigned-url-expiration: 5 # minutos